{
    "file": "inventory_recommendations.csv",
    "description": "Inventory restocking recommendations generated by comparing predicted demand and current stock.",
    "columns": {
        "product_id": {
            "type": "string",
            "description": "Unique identifier of the product."
        },
        "decision": {
            "type": "string",
            "description": "Inventory action decision (e.g., REORDER or OK)."
        },
        "recommended_order": {
            "type": "integer",
            "description": "Recommended quantity to reorder (0 if decision is OK)."
        },
        "reason": {
            "type": "string",
            "description": "Short explanation for the inventory decision."
        }
    }
}